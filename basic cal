<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Basic Calculate</title>
</head>
<body>
    <script>
      var a = 10;
      var b = 20;
      //Addition two values
      function add(a, b) {
      return a + b;
}
        console.log("Addition : " + a + " + " + b + " = " + add(a, b));

         //Subtract two values
      function subtract(a, b) {
      return a - b;
}
console.log("Subtraction: " + a + " - " + b + " = " + subtract(a, b));

//Multiply two values
function multiply(a,b) {
    return a * b;
}
console.log("Multiplication :" + a + " * " + b + " = "  + multiply(a,b));

//Division two values
function division(a,b){
    return a / b;
}
console.log("Division :" + a + " / " + b + " = "  + division4(a,b));
    </script>
</body>
</html>
